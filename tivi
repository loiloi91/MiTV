pkg update -y && pkg upgrade -y && pkg install android-tools -y && [ ! -d ~/storage ] && termux-setup-storage
if ! grep -q "alias tivi=" ~/.bashrc; then 
    echo "alias tivi='./mitools'" >> ~/.bashrc
fi
curl -sL https://github.com/loiloi91/MiTV/raw/refs/heads/main/mitools -o mitools
chmod +x mitools
./mitools
